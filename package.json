{
  "name": "bgm-winner",
  "version": "1.0.0",
  "private": true,
  "description": "BGM Winner - Sistem Pengelolaan Lapak Pasar",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "cd backend && npx tsx src/index.ts",
    "setup:backend": "cd backend && (test -f .env || cp .env.example .env) && npm install && npx prisma generate && npx prisma db push --accept-data-loss",
    "seed": "cd backend && npx tsx prisma/seed.ts",
    "build:frontend": "cd frontend && npm install && npm run build",
    "start:backend": "cd backend && npx tsx src/index.ts",
    "build": "npm run setup:backend && npm run seed && npm run build:frontend",
    "dev": "npm run setup:backend && npm run seed && npm run start:backend"
  }
}
